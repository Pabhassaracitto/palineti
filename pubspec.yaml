# packages/pali_course/pubspec.yaml
name: pali_course
description: "Pāḷi language learning content — VipLang Method. 26 lessons from Nārada Mahāthera's Elementary Pāḷi Course."
version: 0.1.0
homepage:

environment:
  sdk: ^3.4.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
